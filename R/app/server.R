# Estructura sugerida para servidor de la app Shiny
#
# Nota:
# Este proyecto es un paquete R, por lo que las funciones operativas
# siguen viviendo en `R/app_server.R` para mantener la carga del namespace.
#
# Usa este archivo como punto de organización para el servidor principal.
