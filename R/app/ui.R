# Estructura sugerida para UI de la app Shiny
#
# Nota:
# Este proyecto es un paquete R, por lo que las funciones operativas
# siguen viviendo en `R/app_ui.R` para mantener la carga del namespace.
#
# Usa este archivo como punto de organización para la UI principal.
